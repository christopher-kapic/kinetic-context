{
  "name": "cli",
  "type": "module",
  "scripts": {
    "build": "tsdown",
    "check-types": "tsc -b",
    "compile": "bun build --compile --minify --sourcemap --bytecode ./src/index.ts --outfile kctx",
    "dev": "tsx watch src/index.ts"
  },
  "dependencies": {
    "@kinetic-context/api": "workspace:*",
    "@orpc/client": "catalog:",
    "@clack/prompts": "^0.10.0",
    "citty": "^0.2.1",
    "picocolors": "^1.1.1"
  },
  "devDependencies": {
    "@kinetic-context/config": "workspace:*",
    "@types/node": "catalog:",
    "tsdown": "^0.16.5",
    "tsx": "^4.19.2",
    "typescript": "catalog:"
  }
}
